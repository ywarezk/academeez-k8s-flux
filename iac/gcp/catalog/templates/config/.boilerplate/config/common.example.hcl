#
# Common configuration shared across environments.
# Copy to common.hcl and customize with your values,
# or run `terragrunt scaffold` from the live directory (see README.md).
#

locals {
  org_id         = "XXXXXX"
  common_project = "XXXXXXX"
  customer_id    = "XXXXXXX"
}
