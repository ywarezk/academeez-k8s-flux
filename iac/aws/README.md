# Amazon Web Services (AWS)

AWS support is not implemented yet. The intended layout mirrors GCP:

```
aws/
├── catalog/   # reusable Terragrunt units
└── live/      # live environment
```

Contributions welcome — open a PR with the `aws/` folder structure above.
